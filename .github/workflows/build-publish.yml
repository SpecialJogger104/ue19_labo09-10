name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up QEMU
        run: docker run --rm --privileged multiarch/qemu-user-static --reset -p yes

      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: List contents of the directory
        run: Get-ChildItem "C:\Users\Grégory\OneDrive\Documents\GitHub\ue19_labo09-10"
        shell: powershell
      
      - name: Build and push Docker image
        run: |
          docker buildx create --use
          docker buildx inspect default --bootstrap
          docker buildx build --platform linux/arm64 -t docker-image C:/Users/Grégory/OneDrive/Documents/GitHub/ue19_labo09-10
          docker buildx build --platform linux/arm64 -t docker-image --load C:/Users/Grégory/OneDrive/Documents/GitHub/ue19_labo09-10
          docker run -p 8080:80 --platform linux/arm64 docker-image

